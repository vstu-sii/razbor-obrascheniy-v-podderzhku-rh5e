# Кандидаты моделей и токен-бюджет

Дата проверки источников и тарифов: 17.09.2026.

## Кандидат 1. Облачная модель OpenAI со Structured Outputs

Роль: основной облачный baseline для проверки качества структурированной карточки.

Почему рассматриваем: официальный API поддерживает результат по JSON Schema. Это позволяет автоматически валидировать поля карточки: [OpenAI Responses — Structured Outputs](https://developers.openai.com/api/reference/cli/resources/beta/subresources/responses#responses-beta_response_format_text_config).

Риски: внешний API, необходимость проверить доступность конкретной модели для аккаунта, стоимость и требования к обработке данных. Название модели и актуальный тариф нельзя фиксировать без проверки аккаунта команды.

Что измеряем: качество на одном golden dataset, p95 latency, input/output tokens, стоимость одного triage и долю ответов, прошедших JSON Schema.

## Кандидат 2. GigaChat-2-Max

Роль: русскоязычный облачный кандидат.

Почему рассматриваем: GigaChat поддерживает response_format с типом json_schema, поэтому может возвращать карточку по заданной схеме: [GigaChat — структурированные данные](https://developers.sber.ru/docs/ru/gigachat/guides/structured-output).

На 17.09.2026 официальный корпоративный тариф для синхронного GigaChat Max составляет 0,65 рубля за 1000 токенов: [тарифы GigaChat для юридических лиц](https://developers.sber.ru/docs/ru/gigachat/tariffs/legal-tariffs).

При рабочей оценке 2700 токенов один вызов стоит примерно 1,76 рубля. Если разрешён один полный повторный вызов, верхняя оценка составляет 5400 токенов или примерно 3,51 рубля. Оба значения укладываются в рабочий лимит 5 рублей за triage.

Риски: тариф и доступность могут измениться; нужно отдельно проверить качество retrieval, соблюдение схемы и работу с обезличенными данными.

## Кандидат 3. Qwen2.5-7B-Instruct локально

Роль: локальный резерв, если данные нельзя отправлять облачному провайдеру.

Модель имеет 7,61 млрд параметров, поддерживает длинный контекст и заявляет улучшенную генерацию структурированного JSON: [официальная карточка Qwen2.5-7B-Instruct](https://huggingface.co/Qwen/Qwen2.5-7B-Instruct).

Преимущество: данные остаются в контролируемом контуре.

Риски: нужны подходящее железо, измерение реальной latency и отдельная проверка качества русского языка и JSON Schema. Денежная стоимость одного запроса зависит от доступного оборудования, поэтому до замера её нельзя сравнивать с облачным тарифом.

## Токен-бюджет одного разбора

Инструкция занимает примерно 450 токенов.

Обращение занимает примерно 250 токенов.

История занимает примерно 600 токенов.

Найденные фрагменты занимают примерно 900 токенов.

Результат занимает примерно 500 токенов.

Итого один вызов использует примерно 2700 токенов. Один разрешённый повтор увеличивает верхнюю оценку до 5400 токенов.

Фактическая стоимость считается так: стоимость input-токенов плюс стоимость output-токенов плюс retrieval и embeddings плюс повторные вызовы. Полученную сумму делим на число завершённых разборов.

Рабочий продуктовый лимит — не больше 5 рублей за один завершённый triage.

## Деградация

Если API недоступен, оператор видит исходный тикет и ручной поиск.

Если превышается бюджет, система сокращает нерелевантную историю и явно отмечает сокращение.

Если ответ не соответствует JSON Schema, запрос повторяется один раз, затем тикет передаётся оператору.

Если retrieval не нашёл подтверждённый источник, система не генерирует процедуру и выбирает clarify или escalate.

## НУЖНО ДОБАВИТЬ КОМАНДЕ

- AI Engineer: указать конкретную модель OpenAI, доступную аккаунту команды, и добавить прямую ссылку на её тариф.
- AI Engineer: запустить три кандидата на одинаковых примерах и вписать фактические quality, latency, tokens и cost.
- AI Engineer: указать оборудование и стоимость локального прогона Qwen.
- Product: подтвердить лимит 5 рублей за triage.
